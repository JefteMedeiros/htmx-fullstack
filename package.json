{
  "name": "htmx",
  "version": "1.0.0",
  "description": "A monolith project using htmx and express",
  "scripts": {
    "styles": "tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "start": "nodemon server.js",
    "dev": "npm run start && npm run styles"
  },
  "keywords": [],
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.3.3"
  },
  "dependencies": {
    "express": "^4.18.2",
    "htmx": "^0.0.2",
    "nodemon": "^3.0.1"
  }
}
